# Supplied ORIENT numerical water fixture

User-authorized bounded snapshots from ONLY
`/home/awallace43/gits/camcasp_psi4/.camcasp-reference/work/H2O`.
No H2O-isagrid, historical cDF output_2, source algorithms, or executables are copied.
Attribution: external ORIENT 5.0.10 (d8d8610) localization and external CamCASP/PFIT
refinement; original filenames and input SHA256s are retained. Reading numerical
output is not copying the GPL localization algorithm; this is not blanket licensing
clearance for other source material or redistribution.

`H2O_ref_wt4_L3_0f10.pol` is a byte-identical 11-frequency, 3-site, dense 15x15
NEW-format collection. `H2O_ref_wt4_L3_casimir.data` is independently serialized
numeric input for comparison, not an oracle regenerated by the .pol parser.
The four small .ornt/.axes/.sites/.pdef files are input configuration/provenance,
not algorithms. All 45 audited adjacent source SHA256s verified; the detailed
source inventory is `.pi/audit/orient-bridge-input-hashes.json` in the worktree.

`manifest.json` schema 1 is the explicit external authority. Its adjacent SHA256
pins the exact declaration, including units, geometry, frames, source hashes, and
section-to-grid mapping. A hash verifies identity, not producer correctness.
All refined raw INDEX/FREQSQ headers are zero; enclosing `# INDEX 000..010` maps to
`core.CasimirGrid(10,0.5)`, static index0, and cp_weight including 1/(2*pi) once.
No raw tensor is deduplicated, symmetrized, clipped, localized, or refined here.
All33 refined tensors are exactly reciprocal; 22 have negative eigenvalues.

Frames are proper local-to-global Cartesian matrices with axes as columns:
O identity (explicit declaration, unrotated molecule at global origin, no O axes
override); H1 diag(-1,-1,1); H2 identity, from global Z and H2-to-H1/H1-to-H2 x.
Sites are bohr. Dipole real Racah components are z,x,y, converted to xyz then F A F^T.
Higher ranks remain real Racah; anisotropic rotation uses existing Psi4 kernels.

`frequency_header_excerpt.json` contains only literal numerical headers and source
hashes from all11 NL4/unrefined files. Distributed NL4 tensors are NOT local inputs.
Unrefined indices7..10 fail the literal seven-decimal comparison (upstream rounding
may precede re-serialization); these strict failures stay recorded. They do not
replace canonical quadrature nodes or suppress valid refined-output calculations.
`pot_isotropic_excerpt.json` retains three literal 00/00/J=0 rows from the archived
C12 potential, with source hashes/line numbers. Compare only the rotational scalar
to isotropic even orders, with exact H1/H2 scalar equivalence checked for type H.
Directional coefficients must not be compared to recoupled angular components.
Rank3 C12 is partial even where the archive calls it C12 without qualification.

`placement_b_example.json` explicitly places a second copy at translation(0,0,10)
bohr with identity rotation. This is an EXAMPLE PAIR GEOMETRY, not reference geometry
agreement. Output origins and frames both transform. No implicit self-placement.

Runtime needs only these portable fixtures and staged Python plus the existing
core APIs. CLI: `../oracle/run_supplied_local_properties.py`; handoff has exact
parent-only staging/test/production commands. No native or wavefunction-first
acceptance is inferred; internal localization/refinement and numerical corrections
remain explicit TODOs.

## Hermetic LW localization input/output (strict checkpoint, currently blocked)

`lw-hermetic-water.json` and its adjacent SHA256 contain literal decimal tokens
from ONLY `H2O_NL4_000.pol` (SHA256
`9b6130f42fc50b50b80d5860f13cc6b5b198002c4c74a1b3500893481502c166`)
and unrefined `H2O_L3_000.pol` (SHA256
`9c027937ffe3eb8c80c67015dc927983ca70464bd7e265e0f417eb59a5114172`)
in the authorized historical H2O track above. All nine ordered 25x25 distributed
blocks (5625 entries) and all three 15x15 expected local blocks (675 entries) are
retained, including tiny raw asymmetry and signed-zero tokens. No symmetrization,
clipping, fitting, or candidate-generated expected values occur. This is not the
PFIT/WSM-refined `H2O_ref_wt4_L3_0f10.pol` oracle.

The opt-in `../oracle/extract_lw_hermetic.py` reads only those two named files and
validates their reviewed hashes, every header, ordered label/index identity,
ranks, static frequency, dimensions, numerical tokens, and END/ENDFILE structure.
NL4 explicitly declares `CARTSPHER S`; NEW-format L3 has no CARTSPHER or numeric
site-index fields, so spherical representation is declared by provenance and
site identity by its diagonal SITE-NAMES headers. Both raw static indices are 1,
not the refined collection's index convention. Full-file consumption is required.
Extractor/source hashes and extraction provenance accompany the numbers; hashes
establish identity, not producer correctness. No GPL source was read or copied.

Distributed NL4 is still **never a local input**: the new test supplies its exact
top-left 16x16 blocks (2304 entries, rank 0 retained; 3321 rank-4-containing entries
discarded) to native `isa_localize_lw`, frequency 0, explicit graph O-H1/O-H2.
Origins and frames come only from the unchanged, hash-pinned portable manifest,
H2O.sites and H2O.axes. Only the manifest's geometry/frame declaration is reused,
not its refined-tensor origin/grid mapping. Global output is converted to local
using `D(F)^T A D(F)` with the ranks 1..3 block of `isa_multipole_rotation(3,F)`;
H1 has F=diag(-1,-1,1), O/H2 identity. The runtime test never opens reference paths
or invokes extraction tools. Malformed-parser tests use portable literals only.

`test_isapol_lw_hermetic.py` keeps the default **1e-6 postcondition** distinct from
**1e-11 absolute, rtol=0 over all 675 expected entries**. The production default
still FAILS and its rejection has a dedicated regression. Exact decimal input
charge sums already reach7.011e-4 at H1/H2 component32c; this is not an output
error and is not fixed by rotating H1. User explicitly authorized a separate
**historical-only1e-3 diagnostic**, not a production-default change or automatic
retry. Under that policy all675 output entries PASS: maximum absolute errors
O6.252776074688882e-13, H1 5.60440582830779e-13, H2 5.089262344881718e-13.
The27 tests also include fixture-only and candidate-output negative H1 frame
controls; omitting the rotation produces a difference greater than15.
No reference values, kernel defaults, or1e-11 comparison thresholds changed;
no xfail/skip was added. Measured residuals are off_site2.201454e-11,
charge_sum0.000701099999999899, reciprocity5.771383371211414e-12,
molecular_sum3.979039320256561e-13, local_charge0.0007011000000002321.

Scope: native LW processing of supplied nonlocal tensors with historical output
agreement, NOT production postcondition acceptance, native end-to-end water
properties, fresh wavefunction generation, or native PFIT. The initial strict
failure is retained in `.pi/audit/lw-hermetic-resume-handoff.md`; subsequent
user-authorized measurements are in `lw-hermetic-diagnostic-v2.log`.
